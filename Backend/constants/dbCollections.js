/**
 * @file dbCollections.js
 * @description MongoDB collection name constants.
 *              Prevents typos by centralizing collection name strings.
 */

const DB_COLLECTIONS = {
  USERS: 'users',
  EARTHQUAKES: 'earthquakes',
};

module.exports = DB_COLLECTIONS;
